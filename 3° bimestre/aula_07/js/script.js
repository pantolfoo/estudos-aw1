console.log("isinha"); // aparece no console do site (inspecionar)

var nome = "isabella"; // declaracao de variavel (opcionalmente, inicializando-a com um valor) || global e local
let idade = 17; // declaracao de variavel (opcionalmente, inicializando-a com um valor) || local - escopo de boloco
const PI = 3.14; // declara variavel constante]
var numero = 2;


// alerta:
// window.alert ('criou??')

// funcao:

document.write(nome);